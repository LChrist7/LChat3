{
  "name": "messenger-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static host for Messenger (HTML + Firebase + SW) on Render",
  "scripts": {
    "start": "serve -s -l $PORT .",
    "start:local": "serve -s . -l 5173"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
